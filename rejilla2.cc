#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

vector<vector<vector<pair<ll,ll>>>>v;
void dfs(int x, int y) {

}

int main(){
	ll t, n, m, a;
	cin >> t;
	while (t--) {
		cin >> n >> m;
		v = vector<vector<vector<pair<ll,ll>>>>(n, vector<vector<pair<ll,ll>>>(m));
		
	}
}