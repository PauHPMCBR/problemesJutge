#include <iostream>
using namespace std;

int main(){
    unsigned long long int n, m;
    while (cin >> n >> m) {
      if (n == m) cout << "Javier" << endl;
      else cout << "Max" << endl;
    }
}
