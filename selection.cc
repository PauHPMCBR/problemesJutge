#include <bits/stdc++.h>
using namespace std;
int n,m;
vector<int>v;

int bs(int esq, int dre) {
	
}

int main(){
	cin >> n >> m;
	v = vector<int>(n);
	for (int i = 0; i < n; ++i) cin >> v[i];

}