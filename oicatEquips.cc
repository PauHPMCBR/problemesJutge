#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	while (cin >> n) {
		vector<int>q(n);
		vector<int>g(n);
		vector<int>c(n);
		for (int i = 0; i < n; ++i) {
			cin >> q[i] >> g[i] >> c[i];
		}
	}
}