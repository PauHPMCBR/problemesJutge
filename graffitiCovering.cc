#include <bits/stdc++.h>
using namespace std;


int main() {
    int g, p;
    while (cin >> g) {
        vector <int> v(g);
        for (int i = 0; i < g; ++i) cin >> v[i];
        cin >> p;
        if (p >= g) cout << p << endl;
        else {
            sort(v.begin(). v.end());
            
        }
    }
}
